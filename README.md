# YAML Indent Visualizer & Best Practices

Расширение для VS Code для работы с YAML и Ansible файлами:
- 🔍 Проверка синтаксиса с yamllint
- 🎯 Best practices с ansible-lint  
- 📝 Автоматическое исправление отступов
- 👁️ Визуализация иерархии родительских ключей
- ⚡ Автоматическая установка всех зависимостей

## 🚀 Быстрый старт

### Автоматическая установка (рекомендуется)

1. Установите расширение
2. При первом запуске появится уведомление:
   ```
   Не найдены инструменты: yamllint, ansible-lint, pre-commit
   Установить автоматически?
   ```
3. Нажмите **"Установить"**
4. Перезапустите VS Code
5. Готово! ✅

### Требуемые инструменты

Расширение автоматически установит:
- ✅ **ansible** - Ansible core
- ✅ **yamllint** - проверка синтаксиса YAML
- ✅ **ansible-lint** - проверка best practices Ansible
- ✅ **pre-commit** - pre-commit hooks (обязательно!)

## 📦 Ручная установка зависимостей

### Linux/macOS:
```bash
pip3 install --user ansible yamllint ansible-lint pre-commit
```

### Windows:
```bash
pip install ansible yamllint ansible-lint pre-commit
```

### Через apt (Debian/Ubuntu/Astra):
```bash
sudo apt-get install -y ansible yamllint ansible-lint pre-commit
```

### Через pipx (изолированная установка):
```bash
pipx install ansible --include-deps
pipx install yamllint
pipx install ansible-lint
pipx install pre-commit
```

## 🎯 Возможности

- **Визуализация иерархии** - подсветка родительских ключей при навигации
- **Автоматическая проверка** - при открытии панели запускаются все проверки
- **yamllint** - синтаксис, отступы, длина строк
- **ansible-lint** - best practices для Ansible playbooks
- **pre-commit** - интеграция с pre-commit hooks
- **Автоисправление** - одна кнопка для применения всех исправлений

## 📚 Документация

- [Linux установка](LINUX_SETUP.md)
- [Быстрый старт](QUICK_START_LINUX.md)
- [Работа с venv](VENV_SETUP.md)
- [Автоматическая установка](AUTO_INSTALL.md)
- [Debian/Astra исправление](DEBIAN_INSTALL_FIX.md)
- [Pre-commit](PRE_COMMIT_OPTIONAL.md)

## 🐛 Устранение проблем

См. [DEBIAN_INSTALL_FIX.md](DEBIAN_INSTALL_FIX.md) для решения проблемы `externally-managed-environment`

## 📄 Лицензия

MIT