# 🚀 Быстрая сборка VSIX

Этот каталог предназначен для хранения готовых `.vsix` пакетов.

---

## ⚡ Быстрый старт (3 команды)

```bash
# 1. Установить зависимости (если еще не установлены)
npm install

# 2. Скомпилировать TypeScript
npm run compile

# 3. Создать VSIX пакет
npm run package
```

Готово! Файл `ansible-lint-helper-1.0.0.vsix` будет создан в корне проекта.

---

## 📦 Создание VSIX с указанием выходного файла

```bash
# Сохранить в build/
npx vsce package --out build/ansible-lint-helper-1.0.0.vsix
```

---

## 🧪 Тестирование

### Установить локально
```bash
code --install-extension build/ansible-lint-helper-1.0.0.vsix
```

### Или через VS Code UI
1. `Ctrl+Shift+P` (или `Cmd+Shift+P` на Mac)
2. `Extensions: Install from VSIX...`
3. Выберите файл из `build/` каталога

---

## ⚠️ Перед созданием VSIX

### 1. Проверьте package.json

Убедитесь, что заполнены:
- `publisher` - ваше имя publisher
- `author` - ваше имя
- `version` - текущая версия

### 2. Обновите CHANGELOG.md

Добавьте описание изменений для текущей версии.

### 3. Скомпилируйте код

```bash
npm run compile
```

### 4. Проверьте что будет включено

```bash
npx vsce ls
```

---

## 📋 Checklist перед релизом

- [ ] Обновлен `version` в `package.json`
- [ ] Обновлен `CHANGELOG.md`
- [ ] Код скомпилирован (`npm run compile`)
- [ ] Нет TypeScript ошибок
- [ ] Проверено содержимое пакета (`vsce ls`)
- [ ] Создан VSIX файл
- [ ] VSIX протестирован локально
- [ ] Создан git tag для версии

---

## 🎯 Структура build/

```
build/
├── README.md                           # Этот файл
├── ansible-lint-helper-1.0.0.vsix      # VSIX пакет
├── ansible-lint-helper-1.0.1.vsix      # Следующая версия
└── ...
```

---

## 📚 Полная документация

См. [BUILD_INSTRUCTIONS.md](../BUILD_INSTRUCTIONS.md) в корне проекта для подробной информации.

---

## 🔗 Полезные команды

```bash
# Проверка версии vsce
npx vsce --version

# Список файлов в пакете
npx vsce ls

# Создание пакета с pre-release версией
npx vsce package --pre-release

# Публикация (требуется PAT)
npx vsce publish

# Удаление расширения
code --uninstall-extension your-publisher.ansible-lint-helper
```

---

## 🎉 Готово к распространению!

После создания `.vsix` файла вы можете:

✅ Установить локально для тестирования
✅ Отправить коллегам
✅ Загрузить на GitHub Releases
✅ Опубликовать в VS Code Marketplace
