# Быстрый запуск на Linux (Astra Linux)

## ⚡ Вы здесь (уже выполнено):

```bash
✅ npm install
✅ npm run compile
```

## 🚀 Следующие шаги:

### Вариант 1: Запуск в режиме разработки (РЕКОМЕНДУЕТСЯ)

1. **Откройте проект в VS Code:**
   ```bash
   # Убедитесь что вы в директории проекта
   cd ~/Desktops/Desktop1/ansible_formatter_vscode
   
   # Откройте VS Code ИМЕННО В ЭТОЙ ДИРЕКТОРИИ
   code .
   ```

2. **Запустите Extension Development Host:**
   - Нажмите **F5** (или Debug → Start Debugging)
   - Или нажмите **Ctrl+Shift+D** и выберите "Run Extension", затем зеленую кнопку
   
3. **Откроется НОВОЕ окно VS Code с заголовком "[Extension Development Host]"**
   - Это специальное окно для тестирования расширения
   - В ЭТОМ окне расширение будет активно

4. **В новом окне откройте YAML файл:**
   ```bash
   File → Open File → test_extension/main.yml
   ```

5. **Откройте панель расширения:**
   - Найдите иконку в левой боковой панели (Activity Bar)
   - Или: View → Open View → "YAML Indent: Indent Preview"

### Вариант 2: Установка как расширение

Если хотите установить расширение в обычный VS Code:

```bash
# 1. Установите vsce (если нет)
sudo npm install -g @vscode/vsce

# 2. Создайте VSIX пакет
vsce package

# 3. Установите расширение
code --install-extension yaml-indent-visualizer-0.1.0.vsix

# 4. Перезапустите VS Code
```

## 🔍 Проверка что расширение работает

### После F5 должно открыться НОВОЕ окно:

```
┌─────────────────────────────────────────────────────┐
│ Visual Studio Code [Extension Development Host]     │  ← НОВОЕ ОКНО!
├─────────────────────────────────────────────────────┤
│ В этом окне расширение активно                      │
│                                                      │
│ 1. Откройте YAML файл                               │
│ 2. Найдите иконку "YAML Indent" слева               │
│ 3. Откроется панель с проверкой                     │
└─────────────────────────────────────────────────────┘
```

## 🐛 Если не запускается:

### Проверка 1: VS Code открыт в правильной директории

```bash
# В терминале VS Code (Ctrl + `)
pwd
# Должно быть: .../ansible_formatter_vscode
```

### Проверка 2: Есть ли файл launch.json

```bash
ls -la .vscode/launch.json
# Должен существовать
```

### Проверка 3: Проверьте вывод компиляции

```bash
# Должны быть файлы в out/
ls -la out/
# Должны быть: extension.js, indentFixer.js, previewProvider.js
```

### Проверка 4: Смотрим логи

В окне Extension Development Host:
- Help → Toggle Developer Tools
- Console → ищите ошибки

## 📸 Как должно выглядеть:

### Шаг 1: Нажали F5
```
[Информация] Запуск расширения...
[Информация] Extension activated
```

### Шаг 2: Открылось новое окно
```
Заголовок окна: [Extension Development Host]
```

### Шаг 3: В Activity Bar появилась иконка
```
┌──┐
│📁│ Explorer
│🔍│ Search
│⚙️│ Source Control
│🐛│ Run and Debug
│📊│ YAML Indent  ← НАША ИКОНКА!
└──┘
```

### Шаг 4: Открыли панель - видите проверки
```
🔍 Best Practices & Lint
   📋 yamllint: ...
   🔍 ansible-lint: ...

📝 Форматирование
   ⚠ Найдены проблемы...
```

## 💡 Важно!

- **Extension Development Host** - это ОТДЕЛЬНОЕ окно VS Code
- В ОБЫЧНОМ окне VS Code (где открыт исходный код) расширение НЕ будет работать
- Расширение работает ТОЛЬКО в окне с заголовком "[Extension Development Host]"

## 🔄 Если нужно перезапустить:

1. Закройте окно Extension Development Host
2. Вернитесь в окно с кодом
3. Нажмите F5 снова

## ❓ Частые вопросы:

**Q: Я нажал F5, но ничего не происходит**
```bash
A: Проверьте что VS Code открыт именно в директории проекта:
   code ~/Desktops/Desktop1/ansible_formatter_vscode
```

**Q: Окно открылось, но расширения нет**
```bash
A: Проверьте:
   1. Есть ли иконка в Activity Bar (слева)
   2. View → Open View → найдите "YAML Indent"
   3. Откройте YAML файл сначала
```

**Q: Вижу ошибку "Cannot find module"**
```bash
A: Переустановите зависимости:
   rm -rf node_modules out
   npm install
   npm run compile
```

**Q: Хочу чтобы работало в обычном VS Code**
```bash
A: Используйте Вариант 2 - установка VSIX:
   vsce package
   code --install-extension yaml-indent-visualizer-0.1.0.vsix
```

## 📞 Если все еще не работает:

Отправьте вывод команд:

```bash
# 1. Текущая директория
pwd

# 2. Содержимое out/
ls -la out/

# 3. Версия VS Code
code --version

# 4. Содержимое package.json (первые строки)
head -n 20 package.json
```
