# 📦 Инструкция по созданию VSIX пакета

Это руководство покажет, как упаковать расширение в `.vsix` файл для распространения.

---

## 🎯 Предварительные требования

1. **Node.js и npm** должны быть установлены
2. **TypeScript** должен быть скомпилирован
3. Все зависимости установлены (`npm install`)

---

## 📋 Шаг 1: Установите vsce (Visual Studio Code Extension Manager)

```bash
npm install
```

Это установит `@vscode/vsce` из `devDependencies`.

Или глобально:
```bash
npm install -g @vscode/vsce
```

---

## 📝 Шаг 2: Обновите package.json

**Обязательно измените** перед публикацией:

```json
{
  "publisher": "your-publisher-name",  // ← Ваше имя publisher
  "author": {
    "name": "Your Name"                // ← Ваше имя
  }
}
```

### Если публикуете в VS Code Marketplace:

1. Создайте аккаунт на https://marketplace.visualstudio.com/
2. Создайте Personal Access Token (PAT)
3. Зарегистрируйте publisher:
   ```bash
   vsce create-publisher <publisher-name>
   ```
4. Войдите:
   ```bash
   vsce login <publisher-name>
   ```

---

## 🔨 Шаг 3: Компиляция TypeScript

Скомпилируйте код:

```bash
npm run compile
```

Это создаст скомпилированные `.js` файлы в папке `out/`.

---

## 📦 Шаг 4: Создание VSIX пакета

### Вариант 1: Через npm script

```bash
npm run package
```

### Вариант 2: Напрямую через vsce

```bash
npx vsce package
```

### С указанием версии

```bash
npx vsce package --out build/ansible-lint-helper-1.0.0.vsix
```

---

## ✅ Результат

После успешного выполнения вы получите файл:

```
ansible-lint-helper-1.0.0.vsix
```

Этот файл можно:
- ✅ Установить локально в VS Code
- ✅ Отправить коллегам
- ✅ Опубликовать в Marketplace (если есть publisher)

---

## 🧪 Шаг 5: Тестирование VSIX

### Установка локально

```bash
code --install-extension ansible-lint-helper-1.0.0.vsix
```

Или через VS Code:
1. `Ctrl+Shift+P` → `Extensions: Install from VSIX...`
2. Выберите `.vsix` файл

### Проверка

1. Перезапустите VS Code
2. Откройте YAML/Ansible файл
3. Проверьте работу:
   - Кнопка "Run Ansible Lint" в Status Bar
   - Панель "Ansible Lint Results" слева
   - Команды в Command Palette (`Ctrl+Shift+P`)

---

## 📤 Шаг 6: Публикация (опционально)

### В VS Code Marketplace

```bash
npm run publish
```

Или:

```bash
npx vsce publish
```

### На GitHub Releases

1. Создайте release на GitHub
2. Прикрепите `.vsix` файл к release
3. Пользователи смогут скачать и установить

---

## 🔍 Проверка содержимого VSIX

Чтобы посмотреть, что попало в пакет:

```bash
npx vsce ls
```

Это покажет список файлов, которые будут включены в `.vsix`.

---

## ⚠️ Частые проблемы

### 1. Missing publisher name

```
ERROR  Missing publisher name. Learn more: https://...
```

**Решение:** Добавьте `"publisher": "your-name"` в `package.json`

### 2. Icon not found

```
ERROR  Icon resources/icon.png not found
```

**Решение:**
- Убедитесь, что файл существует
- Или закомментируйте `"icon"` в `package.json`

### 3. Files not included

Проверьте `.vscodeignore` - он определяет, какие файлы НЕ включать в пакет.

---

## 📊 Структура пакета

```
ansible-lint-helper-1.0.0.vsix
├── extension/
│   ├── out/                    # Скомпилированный JS код
│   │   ├── extension.js
│   │   ├── parser.js
│   │   └── ...
│   ├── resources/              # Ресурсы (иконки)
│   ├── README.md
│   ├── CHANGELOG.md
│   ├── LICENSE
│   ├── package.json
│   └── ...
└── [Content_Types].xml
```

---

## 🎉 Готово!

Теперь у вас есть `.vsix` файл, который можно:
- Установить на любой VS Code
- Отправить пользователям
- Опубликовать в Marketplace

---

## 📚 Дополнительная информация

- [vsce Documentation](https://code.visualstudio.com/api/working-with-extensions/publishing-extension)
- [Extension Manifest](https://code.visualstudio.com/api/references/extension-manifest)
- [Extension Marketplace](https://marketplace.visualstudio.com/vscode)
