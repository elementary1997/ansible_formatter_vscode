# ⚡ Быстрая сборка VSIX

## 🚀 Одна команда для создания VSIX

```bash
npx vsce package --out build/ansible-lint-helper-1.0.0.vsix
```

✅ **Готово!** Файл будет в каталоге `build/`

---

## 📦 Или используйте npm script

```bash
npm run package
```

---

## 🔨 Полный процесс (если нужно)

```bash
# 1. Установить зависимости
npm install

# 2. Скомпилировать TypeScript  
npm run compile

# 3. Создать VSIX
npm run package
```

---

## 🧪 Установка для тестирования

```bash
code --install-extension build/ansible-lint-helper-1.0.0.vsix
```

---

## 📋 Используйте автоматические скрипты

### Windows (PowerShell)
```powershell
cd build
.\build.ps1
```

### Linux/Mac (Bash)
```bash
cd build
chmod +x build.sh
./build.sh
```

---

## 📚 Полная документация

- См. `build/README.md` - Быстрый старт
- См. `BUILD_INSTRUCTIONS.md` - Подробное руководство

---

## ✨ Результат

После успешной сборки:
- ✅ Файл `build/ansible-lint-helper-1.0.0.vsix` создан
- ✅ Размер: ~126 KB
- ✅ 75 файлов внутри пакета
- ✅ Готов к установке и распространению
