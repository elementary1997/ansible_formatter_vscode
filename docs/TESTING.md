# Инструкция по тестированию расширения

## Шаг 1: Проверка зависимостей

Убедитесь, что ansible-lint установлен:

```bash
ansible-lint --version
```

Если не установлен:

```bash
pip install ansible ansible-lint
```

## Шаг 2: Запуск расширения

1. Откройте проект в VSCode
2. Нажмите **F5** для запуска Extension Development Host
3. Откроется новое окно VSCode с активированным расширением

## Шаг 3: Открытие тестового файла

В новом окне VSCode:
1. Откройте файл `test_playbook.yml` из workspace
2. Вы должны увидеть кнопку **"Run Ansible Lint"** в Status Bar (внизу слева)

## Шаг 4: Запуск проверки

**Способ 1: Через Status Bar**
- Нажмите на кнопку **"Run Ansible Lint"**

**Способ 2: Через Command Palette**
- Нажмите `Ctrl+Shift+P` (или `Cmd+Shift+P` на Mac)
- Введите: `Ansible Lint: Run on Current File`
- Нажмите Enter

## Шаг 5: Проверка результатов

После запуска вы должны увидеть:

### 1. Inline ошибки (подчеркивания)
- Строка 2: `hosts: all  ` - trailing spaces
- Строка 8: `name: Start nginx   ` - trailing spaces
- Строка 5: задача без name

### 2. Боковая панель
- Откройте боковую панель **"Ansible Lint"** (слева, иконка с документом)
- Должны отображаться:
  - 📊 Статистика ошибок
  - 📁 Файл test_playbook.yml
  - Список ошибок с возможностью клика

### 3. Problems Panel
- Откройте Problems Panel (`Ctrl+Shift+M`)
- Должны отображаться все найденные ошибки

## Шаг 6: Тестирование Quick Fix

1. **Наведите курсор** на подчеркнутую ошибку (например, trailing spaces)
2. **Нажмите на лампочку** 💡 или `Ctrl+.`
3. Должны появиться варианты:
   - ⚡ Quick Fix: trailing-spaces
   - 🔧 Fix with ansible-lint
   - 🚫 Ignore this rule
   - 📖 Show documentation

4. **Выберите "Quick Fix"**
5. Ошибка должна быть исправлена мгновенно

## Шаг 7: Тестирование Fix File

1. В боковой панели "Ansible Lint" нажмите на файл
2. Нажмите кнопку **"Fix File"**
3. Файл должен быть исправлен:
   - Trailing spaces удалены
   - Отступы исправлены
   - Лишние пустые строки удалены

## Шаг 8: Тестирование Ignore Rule

1. Выберите любую ошибку
2. Нажмите 💡 → **"Ignore this rule"**
3. Должен открыться файл `.ansible-lint` с добавленным правилом в `skip_list`

## Ожидаемые ошибки в test_playbook.yml

Файл специально содержит следующие ошибки:

1. **Trailing spaces** (строки 2, 8)
   - Исправление: Quick Fixer
   - Результат: пробелы удалены

2. **Неправильные отступы** (строка 4)
   - Исправление: Quick Fixer или ansible-lint --fix
   - Результат: tasks имеет отступ 2 пробела

3. **Лишние пустые строки** (строки 11-12)
   - Исправление: Quick Fixer
   - Результат: лишние строки удалены

4. **Задача без name** (строка 5)
   - Исправление: ansible-lint --fix
   - Результат: добавлено поле name

## Проверка всех функций

- ✅ Запуск ansible-lint на текущем файле
- ✅ Отображение ошибок inline
- ✅ Отображение в боковой панели
- ✅ Quick Fix меню
- ✅ Быстрое исправление (Quick Fixer)
- ✅ Исправление через ansible-lint --fix
- ✅ Игнорирование правила
- ✅ Переход к ошибке по клику
- ✅ Кнопка в Status Bar

## Возможные проблемы

### ansible-lint не найден
```
Error: ansible-lint not found
```
**Решение:** Установите ansible-lint: `pip install ansible-lint`

### Нет ошибок в выводе
Если ошибки не появляются, проверьте:
1. Файл действительно содержит ошибки (откройте test_playbook.yml)
2. ansible-lint работает в терминале: `ansible-lint test_playbook.yml`
3. Проверьте Output panel: View → Output → выберите "Ansible Lint Helper"

### Quick Fix не работает
Убедитесь, что:
1. Расширение активировано (должна быть кнопка в Status Bar)
2. Файл открыт и содержит ошибки
3. Курсор находится на подчеркнутой ошибке

## Дополнительное тестирование

### Тест с реальными Ansible файлами

1. Откройте свои реальные Ansible playbooks
2. Запустите ansible-lint
3. Проверьте, что ошибки корректно отображаются
4. Попробуйте исправить файлы

### Тест с pre-commit

Если у вас настроен pre-commit:

1. Откройте Command Palette (`Ctrl+Shift+P`)
2. Выберите: `Ansible Lint: Run pre-commit`
3. Проверьте результаты всех хуков

### Тест на всем проекте

1. Откройте Command Palette
2. Выберите: `Ansible Lint: Run on All Files`
3. Проверьте, что все файлы проверены

## Успешное завершение

Если все тесты прошли успешно, расширение работает корректно! 🎉

Вы можете начать использовать его в своих проектах.
