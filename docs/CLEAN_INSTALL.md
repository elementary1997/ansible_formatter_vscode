# Чистая установка расширения на новом сервере

## 🎯 Инструкция для Linux

### Шаг 1: Подготовка
```bash
# Убедитесь, что установлены все зависимости
python3 --version  # Требуется Python 3.x
node --version     # Требуется Node.js 14+
npm --version      # Требуется npm 6+
code --version     # Требуется VSCode 1.60+

# Установите линтеры
pip3 install yamllint pre-commit ansible ansible-lint

# Проверьте установку
yamllint --version
pre-commit --version
ansible-lint --version
```

### Шаг 2: Клонирование проекта
```bash
# Клонируйте репозиторий
git clone https://github.com/elementary1997/ansible_formatter_vscode.git
cd ansible_formatter_vscode

# Проверьте, что вы на актуальной ветке
git status
git log --oneline -5
```

### Шаг 3: Установка зависимостей
```bash
# Установите npm зависимости
npm install

# Если появятся warnings об устаревших пакетах - это нормально
# Главное, чтобы установка завершилась успешно
```

### Шаг 4: Компиляция
```bash
# Скомпилируйте TypeScript → JavaScript
npm run compile

# Проверьте, что появилась папка out/
ls -la out/
# Должны увидеть: extension.js, parser.js, executor.js и т.д.
```

### Шаг 5: Запуск в режиме разработки
```bash
# Откройте VSCode в корне проекта
code .

# В VSCode нажмите F5 (или Run → Start Debugging)
# Откроется новое окно "Extension Development Host"

# В новом окне откройте ваш Ansible проект
# File → Open Folder → выберите проект с ansible файлами
```

### Шаг 6: Проверка работы
В окне "Extension Development Host":

1. **Откройте файл с ошибками** (например, main.yml с синтаксическими ошибками)

2. **Откройте панель линтера:**
   - View → Open View → "Ansible Lint: Lint Results"
   - Или кликните на иконку Ansible Lint в левой боковой панели

3. **Нажмите кнопку "Run"**

4. **Проверьте результат - должны увидеть:**

```
━━━ YAMLLINT CHECKS ━━━

❌ ERROR [yamllint] syntax                    Line 19:5    📖 Docs
syntax error: expected <block end>, but found '?'

⚠️ WARNING [yamllint] indentation             Line 20:1    📖 Docs
wrong indentation: expected 8 but found 6

━━━ PRE-COMMIT CHECKS ━━━

❌ ERROR [pre-commit] check-yaml              Line 12:1    📖 Docs
while parsing a block collection

━━━ ANSIBLE-LINT CHECKS ━━━

⚠️ WARNING [ansible-lint] name[casing]        Line 15:3    📖 Docs
All names should start with an uppercase letter
🔧 Auto-fixable
```

### ✅ Что должно быть:

- ❌ ⚠️ ℹ️ **Иконки в начале каждой ошибки**
- 🔴 🟡 🔵 **Цветные badges** (ERROR/WARNING/INFO)
- `[yamllint]` `[pre-commit]` `[ansible-lint]` **Источники в тегах**
- **📖 Docs** **ссылки справа от каждой ошибки**
- **🔧 Auto-fixable** **badge для исправляемых ошибок**
- **Только ОДНА панель** "LINT RESULTS"

### ❌ Чего НЕ должно быть:

- ❌ Двух панелей "LINT RESULTS"
- ❌ Простого текста без badges и иконок
- ❌ Ошибок "View provider already registered"
- ❌ Отсутствия кнопок "📖 Docs"

## 🔧 Если что-то не работает

### Проблема 1: Не компилируется
```bash
# Очистите node_modules и переустановите
rm -rf node_modules package-lock.json
npm install
npm run compile
```

### Проблема 2: Не видно улучшенного отображения
```bash
# Убедитесь, что скомпилировали ПОСЛЕ git pull
git pull
npm run compile

# Полностью закройте VSCode и откройте заново
pkill code  # или просто закройте все окна VSCode
code .
# Нажмите F5
```

### Проблема 3: Две панели "LINT RESULTS"
```bash
# Удалите кеш VSCode
rm -rf ~/.config/Code/User/workspaceStorage/*

# Удалите старые версии расширения
rm -rf ~/.vscode/extensions/elementary1997.ansible-lint-helper-*

# Перезапустите VSCode
pkill code
code .
# Нажмите F5
```

### Проблема 4: Нет ссылок "📖 Docs"

1. Откройте **Developer Tools**: Help → Toggle Developer Tools
2. Перейдите на вкладку **Console**
3. Запустите проверку (кнопка "Run")
4. Посмотрите логи - должны быть строки типа:
   ```
   [Parser] yamllint error: {...}
   [Executor] Using workspace config: .yamllint
   ```
5. Если видите ошибки JavaScript - скиньте скриншот

## 📦 Создание VSIX пакета

Если хотите установить как обычное расширение:

```bash
# Соберите VSIX
npm run compile
npx vsce package

# Установите VSIX
code --install-extension ansible-lint-helper-1.0.0.vsix

# Перезапустите VSCode
```

## 🐛 Отладка

### Включите логи расширения:
1. Help → Toggle Developer Tools
2. Console tab
3. Фильтр: `ansible` или `Parser` или `Executor`

### Проверьте версии инструментов:
```bash
yamllint --version    # >= 1.26.0
pre-commit --version  # >= 2.0.0
ansible-lint --version # >= 6.0.0
node --version        # >= 14.0.0
code --version        # >= 1.60.0
```

### Проверьте пути к линтерам:
```bash
which yamllint      # должен вернуть путь
which pre-commit    # должен вернуть путь
which ansible-lint  # должен вернуть путь
```

## 📝 Логи при успешном запуске

В Console должны появиться такие логи:

```
[Extension] Ansible Lint Helper is now active!
[Executor] Found config: /path/to/.yamllint
[Executor] Using workspace config: .yamllint
[Parser] yamllint error: {file: "main.yml", line: 19, ...}
[Executor] Using workspace config: .ansible-lint
[Parser] Pre-commit: found 2 errors
```

## 🎉 Успешная установка

Если все работает правильно, вы увидите:

1. ✅ **Одну** панель "Ansible Lint: Lint Results"
2. ✅ Красивые ошибки с иконками, badges и ссылками на Docs
3. ✅ Кликабельные ссылки "📖 Docs" рядом с каждой ошибкой
4. ✅ Badges "🔧 Auto-fixable" для исправляемых ошибок
5. ✅ Переход к строке при клике на ошибку
6. ✅ Работающие кнопки Run, Check All, Fix All, Clear

---

**Готово! Теперь у вас должна быть чистая установка с полным функционалом! 🚀**

Если проблемы остаются - пришлите:
1. Скриншот панели с ошибками
2. Скриншот Console (Developer Tools)
3. Вывод команды `git log --oneline -5`
