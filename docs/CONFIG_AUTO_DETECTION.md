# Автоматическое определение конфигурационных файлов

## Обзор

Расширение **Ansible Lint Helper** автоматически определяет и использует конфигурационные файлы для линтеров:

- `.yamllint` - для yamllint
- `.ansible-lint` - для ansible-lint
- `.pre-commit-config.yaml` - для pre-commit hooks

## Как это работает

### 1. **Поиск конфигов в workspace**

При запуске линтеров расширение сначала ищет конфигурационные файлы в корне вашего проекта (workspace root).

Поддерживаемые имена файлов:
- `.yamllint`, `yamllint`, `yamllint.yaml`, `yamllint.yml`
- `.ansible-lint`, `ansible-lint`, `ansible-lint.yaml`, `ansible-lint.yml`
- `.pre-commit-config.yaml`, `pre-commit-config.yaml`

### 2. **Использование дефолтных конфигов**

Если конфигурационный файл **не найден** в workspace, расширение автоматически:

#### Для yamllint:
- Использует дефолтный конфиг из расширения (через флаг `-c`)
- Не создает файлы в вашем проекте

#### Для ansible-lint и pre-commit:
- **Копирует** дефолтный конфиг в корень вашего проекта
- Показывает уведомление: `📋 Created default config in workspace`
- Вы можете **настроить** этот конфиг под свои нужды

### 3. **Дефолтные конфиги**

Дефолтные конфиги находятся в папке `defaults/` расширения и включают:

#### `.yamllint` (дефолтный)
- Максимальная длина строки: **150 символов**
- Отступы: **2 пробела**
- Строгая проверка (без исключений)
- Unix-style line endings
- Best practices для Ansible

#### `.ansible-lint` (дефолтный)
- Профиль: **production**
- Валидация структуры проекта
- Исключения: `.cache/`, `venv/`, `node_modules/`, `out/`, `build/`
- Warnings для некритичных правил

#### `.pre-commit-config.yaml` (дефолтный)
- `trailing-whitespace` - удаление пробелов в конце строк
- `end-of-file-fixer` - добавление newline в конце файла
- `check-yaml` - проверка синтаксиса YAML
- `mixed-line-ending` - исправление смешанных окончаний строк
- `check-merge-conflict` - проверка маркеров конфликтов
- `check-case-conflict` - проверка конфликтов имен файлов

## Преимущества

✅ **Без настройки** - расширение работает сразу после установки  
✅ **Гибкость** - можете использовать свои конфиги  
✅ **Best Practices** - дефолтные конфиги следуют лучшим практикам Ansible  
✅ **Прозрачность** - все конфиги видны в вашем проекте (кроме yamllint)

## Примеры использования

### Вариант 1: Использование дефолтных конфигов

```bash
# Создайте новый проект
mkdir my-ansible-project
cd my-ansible-project

# Откройте в VSCode
code .

# Запустите расширение (кнопка "Run" или "Check All")
# Расширение автоматически создаст .ansible-lint и .pre-commit-config.yaml
```

### Вариант 2: Использование своих конфигов

```bash
# Создайте свои конфиги
cat > .yamllint << EOF
---
extends: default
rules:
  line-length:
    max: 200  # Ваше значение
EOF

cat > .ansible-lint << EOF
---
profile: basic  # Ваш профиль
EOF

# Расширение автоматически обнаружит и использует ваши конфиги
```

### Вариант 3: Настройка созданных конфигов

После первого запуска расширение создаст дефолтные конфиги. Вы можете их отредактировать:

```bash
# Отредактируйте созданные конфиги
nano .yamllint
nano .ansible-lint
nano .pre-commit-config.yaml

# При следующем запуске будут использоваться ваши настройки
```

## Расположение дефолтных конфигов

Дефолтные конфиги входят в состав расширения и находятся в:

```
~/.vscode/extensions/elementary1997.ansible-lint-helper-X.X.X/defaults/
├── .yamllint
├── .ansible-lint
└── .pre-commit-config.yaml
```

Вы можете **скопировать** эти конфиги в свой проект и **изменить** под свои нужды.

## Проверка используемого конфига

Чтобы увидеть, какой конфиг используется, откройте **Developer Tools** (Help → Toggle Developer Tools) и посмотрите логи в Console:

```
[Executor] Using workspace config: .yamllint
[Executor] No workspace config found, using default: .ansible-lint
[Executor] Copied default config to /path/to/project/.ansible-lint
```

## FAQ

### Q: Как обновить дефолтные конфиги?
**A:** Обновите расширение до последней версии. Новые проекты будут использовать обновленные дефолты.

### Q: Что делать если конфиг не подходит?
**A:** Создайте или отредактируйте конфиг в корне проекта. Расширение будет использовать ваш конфиг.

### Q: Можно ли использовать конфиг из другой папки?
**A:** Конфиги должны быть в корне workspace. Это стандарт для всех линтеров.

### Q: Как вернуться к дефолтным настройкам?
**A:** Удалите конфиги из проекта (`rm .yamllint .ansible-lint .pre-commit-config.yaml`). При следующем запуске будут использованы дефолты.

## См. также

- [Yamllint Configuration](https://yamllint.readthedocs.io/en/stable/configuration.html)
- [Ansible-lint Configuration](https://ansible-lint.readthedocs.io/configuring/)
- [Pre-commit Configuration](https://pre-commit.com/#plugins)
