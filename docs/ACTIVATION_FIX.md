# Исправление ошибок активации расширения

## Проблема

При запуске расширения вы можете увидеть ошибки:
```
View provider for 'ansible-lint.resultsPanel' already registered
Cannot register multiple views with same id
```

## Причина

Эта ошибка возникает, когда VSCode не полностью очищает старую регистрацию расширения при перезагрузке (Reload Window). Старая версия расширения остается в памяти и конфликтует с новой.

## Решение

### Вариант 1: Полная перезагрузка VSCode (Рекомендуется) ✅

1. **Закройте VSCode полностью**
   - Windows: `Alt+F4` или File → Exit
   - Linux/Mac: `Cmd+Q` или File → Quit

2. **Обновите код:**
   ```bash
   git pull
   npm install
   npm run compile
   ```

3. **Откройте VSCode снова**
   ```bash
   code .
   ```

4. **Нажмите F5** для запуска в режиме разработки

### Вариант 2: Переустановка расширения

Если Вариант 1 не помог:

1. Удалите все версии расширения из VSCode:
   - Extensions (Ctrl+Shift+X)
   - Найдите "Ansible Lint Helper"
   - Uninstall

2. Удалите папку кеша расширений:
   ```bash
   # Windows
   rmdir /s /q "%USERPROFILE%\.vscode\extensions\elementary1997.ansible-lint-helper-*"

   # Linux/Mac
   rm -rf ~/.vscode/extensions/elementary1997.ansible-lint-helper-*
   ```

3. Перезапустите VSCode

4. Установите расширение заново (F5)

### Вариант 3: Очистка workspace storage

Если проблема все еще есть:

1. Закройте VSCode полностью

2. Удалите workspace storage:
   ```bash
   # Windows
   rmdir /s /q "%APPDATA%\Code\User\workspaceStorage"

   # Linux
   rm -rf ~/.config/Code/User/workspaceStorage

   # Mac
   rm -rf ~/Library/Application\ Support/Code/User/workspaceStorage
   ```

3. Откройте VSCode снова

**⚠️ Внимание:** Это удалит все локальные данные workspace (история, состояние панелей и т.д.)

## Проверка исправления

После перезапуска VSCode:

1. ✅ **Ошибка должна исчезнуть** - нет сообщений об ошибках в правом нижнем углу
2. ✅ **Панель "Ansible Lint: Lint Results" должна открыться** в левой боковой панели
3. ✅ **Кнопки должны работать** - Run, Check All, Fix All, Clear
4. ✅ **Ссылки "📖 Docs" должны появиться** у каждой ошибки

## Проверка ссылок Docs

Если ссылки "📖 Docs" не появляются:

1. Откройте файл с ошибками
2. Нажмите **Run** в панели линтера
3. Проверьте, что видите:
   - ❌ ERROR / ⚠️ WARNING / ℹ️ INFO badges
   - `[yamllint]` `[pre-commit]` `[ansible-lint]` источники
   - **📖 Docs** ссылки справа от каждой ошибки
   - **🔧 Auto-fixable** для исправляемых ошибок

4. Кликните на **📖 Docs** → должна открыться документация в браузере

## Если проблема остается

1. Проверьте версию VSCode:
   ```
   Help → About
   ```
   Требуется VSCode >= 1.60.0

2. Проверьте логи:
   - Help → Toggle Developer Tools
   - Console tab
   - Поищите ошибки, связанные с "ansible-lint-helper"

3. Создайте issue на GitHub с:
   - Скриншотом ошибки
   - Версией VSCode
   - Операционной системой
   - Логами из Developer Tools

## Дополнительная информация

- [README.md](README.md) - Основная документация
- [CHANGELOG.md](CHANGELOG.md) - История изменений
- [GitHub Issues](https://github.com/elementary1997/ansible_formatter_vscode/issues) - Сообщить о проблеме
