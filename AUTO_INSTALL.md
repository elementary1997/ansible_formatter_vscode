# 🚀 Автоматическая установка зависимостей

## Как это работает

При первом запуске расширение:

1. ✅ **Проверяет** наличие необходимых инструментов:
   - `yamllint`
   - `ansible-lint`
   - `ansible`

2. 💬 **Показывает уведомление** если что-то отсутствует:
   ```
   Не найдены инструменты: yamllint, ansible-lint
   Установить автоматически?
   ```

3. 🔘 **Даёт выбор:**
   - **Установить** - автоматическая установка через pip
   - **Позже** - напомнит при следующем запуске
   - **Не спрашивать** - больше не показывать это сообщение

## 📝 Что происходит при установке

### Linux / macOS:

Выполняется команда:
```bash
pip3 install --user ansible yamllint ansible-lint
```

После установки показывается напоминание:
```
⚠️ ВАЖНО: Убедитесь что ~/.local/bin в PATH:
echo 'export PATH="$HOME/.local/bin:$PATH"' >> ~/.bashrc
source ~/.bashrc

Затем перезапустите VS Code.
```

### Windows:

Выполняется команда:
```cmd
pip install ansible yamllint ansible-lint
```

## 🔍 Мониторинг установки

Весь процесс установки отображается в отдельном Output канале:

```
View → Output → Select "YAML Indent Visualizer - Установка"
```

Там вы увидите:
- Команду установки
- Прогресс установки
- Установленные пакеты
- Ошибки (если есть)

## ✅ После успешной установки

Появится уведомление:
```
Зависимости установлены!
Перезапустите VS Code для применения изменений.
[Перезапустить]
```

Нажмите **"Перезапустить"** - VS Code перезагрузится и всё заработает!

## 🐛 Если автоматическая установка не сработала

### Проблема 1: "Permission denied"

**Решение для Linux/macOS:**
```bash
# Установите вручную с флагом --user
pip3 install --user ansible yamllint ansible-lint
```

**Решение для Windows:**
```cmd
# Запустите командную строку от администратора
pip install ansible yamllint ansible-lint
```

### Проблема 2: "pip: command not found"

**Решение для Linux:**
```bash
# Ubuntu/Debian
sudo apt install python3-pip

# Fedora/RHEL
sudo dnf install python3-pip

# Arch Linux
sudo pacman -S python-pip
```

**Решение для macOS:**
```bash
# Установите Homebrew (если нет)
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

# Установите Python
brew install python3
```

**Решение для Windows:**
```
Скачайте Python с https://www.python.org/downloads/
Убедитесь что выбрана опция "Add Python to PATH"
```

### Проблема 3: Установилось, но команды не найдены

**Linux/macOS:**
```bash
# Добавьте ~/.local/bin в PATH
echo 'export PATH="$HOME/.local/bin:$PATH"' >> ~/.bashrc
source ~/.bashrc

# Или для zsh
echo 'export PATH="$HOME/.local/bin:$PATH"' >> ~/.zshrc
source ~/.zshrc

# Проверьте
which yamllint
which ansible-lint
```

**Windows:**
```
Добавьте в PATH:
C:\Users\YOUR_USERNAME\AppData\Local\Programs\Python\Python3X\Scripts
```

## 🔄 Сброс проверки зависимостей

Если хотите чтобы расширение снова проверило зависимости:

1. Откройте Command Palette: `Ctrl+Shift+P` (Windows/Linux) или `Cmd+Shift+P` (macOS)
2. Выполните: `Developer: Reload Window`
3. Расширение снова проверит зависимости

## 🎯 Ручная установка (если автоматическая не работает)

### Полная команда для Linux/macOS:

```bash
# Устанавливаем все зависимости
pip3 install --user ansible yamllint ansible-lint pre-commit

# Добавляем в PATH
echo 'export PATH="$HOME/.local/bin:$PATH"' >> ~/.bashrc
source ~/.bashrc

# Проверяем
ansible --version
yamllint --version
ansible-lint --version
```

### Полная команда для Windows:

```powershell
# Устанавливаем все зависимости
pip install ansible yamllint ansible-lint pre-commit

# Проверяем
python -m ansible --version
yamllint --version
ansible-lint --version
```

## 💡 Рекомендации

1. **Используйте автоматическую установку** - это проще всего
2. **Следуйте инструкциям** в Output канале
3. **Перезапускайте VS Code** после установки
4. **Проверяйте PATH** если команды не находятся

## 🔗 Дополнительная информация

- Подробная инструкция для Linux: `LINUX_SETUP.md`
- Работа с venv: `VENV_SETUP.md`
- Быстрый старт: `QUICK_START_LINUX.md`
